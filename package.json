{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/bviral-dashboard run dev",
    "dev:dashboard": "pnpm --filter @workspace/bviral-dashboard run dev",
    "dev:video-editor": "pnpm --dir artifacts/opencut run dev:web",
    "build:video-editor": "pnpm --dir artifacts/opencut run build:web",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
